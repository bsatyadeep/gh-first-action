name: First Workflow
on: [workflow_dispatch]
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Welcome"
      - name: Multi-Line Shell commands
        run: |
          echo "First Output"
          echo "Second Output"
      - name: Print Goodbye
        run: echo "Good - bye!"
